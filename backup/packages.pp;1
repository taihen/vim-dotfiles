class packages {
        package { "nload": ensure => installed }
        package { "htop": ensure => installed }
        package { "less": ensure => installed }
        package { "mtr-tiny": ensure => installed }
        package { "tcpdump": ensure => installed }
        package { "mc": ensure => installed }
        package { "logwatch": ensure => installed }
        package { "ccze": ensure => installed }
        package { "psmisc": ensure => installed }
        package { "curl": ensure => installed }
        package { "fcheck": ensure => purged }
        package { "gpm": ensure => purged }
        package { "vim-tiny": ensure => purged }
        package { "gnutls-bin": ensure => installed }
        package { "git": ensure => installed }

}
